#version 450

layout( binding = 0 ) buffer Data {
   uint n;
   uint phase;
   uint data[];
};

layout( binding = 1 ) buffer Base {
   float base[];
};

layout( binding = 2 ) buffer Result {
   float result[];
};

void main() {
}
